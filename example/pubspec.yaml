name: yaml_lint_example
description: Consumer app used to smoke-test the yaml_lint analyzer plugin.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^5.0.0
  yaml_lint:
    path: ../
